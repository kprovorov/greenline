/**
 * Placeholder for the generated Supabase database schema.
 *
 * Replace with the output of `supabase gen types typescript` once the
 * project's schema exists. Keeping the shape as `Record<string, unknown>`
 * for now means every client factory in this package can be typed as
 * `SupabaseClient<Database>` without call sites having to change later.
 */
export type Database = Record<string, unknown>;
